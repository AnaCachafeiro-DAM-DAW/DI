<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:ListaTareas.MVVMListaTareas.Views;assembly=ListaTareas"
       x:Class="ListaTareas.AppShell">

    <!-- Hamburguesa /  Navegación de routas -->
    <!-- Página principal -->
    <ShellContent Title="Lista Tareas" ContentTemplate="{DataTemplate local:Lista}" />
    <!-- Página de tarea completadas -->
    <ShellContent Title="Completadas" ContentTemplate="{DataTemplate local:Completadas}" />

</Shell>
